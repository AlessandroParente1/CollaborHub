/* Contenitore principale della pagina View */
.view-container {
    display: grid;
    grid-template-columns: 25% 75%; /* Sidebar al 25% e chat al 75% */
    height: 100vh; /* Altezza dell'intera finestra */
    overflow: hidden; /* Nessuno scorrimento globale */
    background-color:#9a8c98;
}

/* Sidebar */
.sidebar {
    background-color: #9A9EBB;
    height: 100%; /* Riempie tutta l'altezza */
    overflow-y: auto; /* Scorrimento verticale per la lista utenti */
    border-right: 1px solid #ddd; /* Separatore laterale */
}

/* Area della chat */
.chat-wrapper {
    position: relative;
    height: 100%; /* Riempie l'altezza disponibile */
    overflow: hidden; /* Nasconde eventuali contenuti fuoriusciti */
    display: flex;
    flex-direction: column;
}

.centered-text {
    display: flex;
    justify-content: center; /* Centra orizzontalmente */
    align-items: center; /* Centra verticalmente (se necessario) */
    text-align: center; /* Assicurati che il testo sia centrato */
    height: 100%; /* Opzionale, dipende dal layout */
}
