/* Stile per il contenitore principale della sidebar */
.sidebar {
    position: relative;
    height: 100vh;
    padding: 16px;
    background-color: #4a4e69; /* Viola scuro */
    color: #f2f2f2; /* Colore del testo chiaro */
    box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
}

/* Titolo della sezione */
.sidebar-title {
    font-size: 1.5rem;
    font-weight: bold;
    margin-bottom: 16px;
    text-align: center;
    color: #ffffff; /* Viola chiaro */
}

/* Lista utenti */
.user-list {
    list-style: none;
    margin: 0;
    padding: 0;
}

.user-list-item {
    display: flex;
    align-items: center;
    padding: 10px;
    margin-bottom: 8px;
    border-radius: 50px;
    transition: background-color 0.3s, transform 0.2s;
    cursor: pointer;
    background-color: #22223b; /* Blu scuro */
}

.user-list-item:hover {
    background-color: #9a8c98; /* Viola chiaro al passaggio */
    color: #22223b; /* Testo scuro */
    transform: scale(1.05);
}

/* Stile del testo dell'utente */
.user-list-item-text {
    font-size: 30px;
    flex-grow: 1;
    display: flex;
    flex-direction: column; /* Cambia l'orientamento in modo da separare il nome e lo stato online */
    justify-content: center; /* Allinea verticalmente il contenuto */
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}


/* Pulsante Logout */
.logout-button {
    margin-top:auto;
    align-self: center;
    bottom: -10px;
    background-color: #ffffff;
    color: #ffffff;
    font-weight: bold;
    border-radius: 8px;
    padding: 10px 20px;
    cursor: pointer;
    transition: background-color 0.3s, transform 0.2s;
}

.logout-button:hover {
    background-color: #ffffff;
    transform: scale(1.1);
}

.online-status {
    color: green;
    margin-left: 10px;
    margin-top: 5px;
    text-align: center;

}

.avatar-image {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    margin-right: 15px;
}

.custom-textfield input {
    color: white; /* Colore del testo */
}

.custom-textfield .MuiOutlinedInput-notchedOutline {
    border-color: white; /* Colore del bordo */
}

.custom-textfield .MuiInputBase-input::placeholder {
    color: white; /* Colore del placeholder */
    opacity: 1; /* Assicurati che il colore sia visibile */
}

